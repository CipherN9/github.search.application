query SearchRepositories($text: String!, $first: Int!) {
  search(type: REPOSITORY, query: $text, first: $first) {
    nodes {
      ... on Repository {
        id: databaseId
        title: name
        owner: owner { login }
        stars: stargazerCount
        description
        url
      }
    }
  }
}